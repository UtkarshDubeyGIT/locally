import { saveFeedbackAction } from "@/app/actions/reports";
import { ReportView } from "@/components/report-view";
import { Button, Card, Label, Select, Textarea } from "@/components/ui";
import { Section } from "@/components/section";
import { getReport } from "@/lib/data";
import { formatDate } from "@/lib/format";
export default async function ClientReport({params}:{params:Promise<{reportId:string}>}){const {reportId}=await params;const d=await getReport(reportId);const r=d.report;const feedback=d.feedback[0];return <><div className="page-head"><div><p className="eyebrow">{formatDate(r.month,{month:"long",year:"numeric"})}</p><h1>{r.clients.business_name}, this was your month.</h1><p>Metrics are a stored snapshot labeled by source. The agency’s next steps are the human interpretation.</p></div></div><ReportView metrics={r.metrics_json} summary={r.agency_summary}/><Section number="03" title="Was this useful?" intro="You can return and update one feedback response for this report."><Card><form action={saveFeedbackAction} className="stack"><input type="hidden" name="reportId" value={r.id}/><Label>Usefulness<Select name="usefulness" defaultValue={feedback?.usefulness??"useful"}><option value="very_useful">Very useful</option><option value="useful">Useful</option><option value="not_useful">Not useful yet</option></Select></Label><Label>What would help next month?<Textarea name="categories" defaultValue={Array.isArray(feedback?.categories_json)?feedback.categories_json.join(", "):"clearer next steps"}/></Label><Label>Comment<Textarea name="comment" defaultValue={feedback?.comment??""}/></Label><Button>Save feedback</Button></form></Card></Section></>}
